<?php
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $id_soporte = $_POST['id_soporte'];

    // Conexión a la base de datos
    // Realizar la eliminación del registro de soporte por su ID
    // Redirigir a una página después de la eliminación
}
?>