<?php
if ($_SERVER['REQUEST_METHOD'] === 'GET') {
    $id_soporte = $_GET['id_soporte'];

    // Conexión a la base de datos
    // Realizar la consulta para obtener el registro de soporte por su ID
    // Mostrar la información del registro de soporte
}
?>