<?php
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $id_calificacion = $_POST['id_calificacion'];

    // Conexión a la base de datos
    // Realizar la eliminación de la calificación por su ID
    // Redirigir a una página después de la eliminación
}
?>