<?php
if ($_SERVER['REQUEST_METHOD'] === 'GET') {
    $id_calificacion = $_GET['id_calificacion'];

    // Conexión a la base de datos
    // Realizar la consulta para obtener la calificación por su ID
    // Mostrar la información de la calificación
}
?>